name: markdown_viewer
description: A simple Markdown viewer application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.1
  file_picker: ^8.0.0
  url_launcher: ^6.2.0
  printing: ^5.11.0
  markdown: ^7.1.1
  pdf: ^3.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
